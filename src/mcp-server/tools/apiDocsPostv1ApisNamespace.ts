/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiDocsPostv1ApisNamespace } from "../../funcs/apiDocsPostv1ApisNamespace.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Postv1ApisNamespaceRequest$inboundSchema,
};

export const tool$apiDocsPostv1ApisNamespace: ToolDefinition<typeof args> = {
  name: "API-docs-postv1-apis-namespace",
  description: `Create an API

Create a new api document specification`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiDocsPostv1ApisNamespace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
