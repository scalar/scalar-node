/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guidesGetv1Guides } from "../../funcs/guidesGetv1Guides.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$guidesGetv1Guides: ToolDefinition = {
  name: "guides-getv1-guides",
  description: `List all projects

Get a list of all guide projects`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await guidesGetv1Guides(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
