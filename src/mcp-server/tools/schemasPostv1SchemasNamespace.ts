/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schemasPostv1SchemasNamespace } from "../../funcs/schemasPostv1SchemasNamespace.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Postv1SchemasNamespaceRequest$inboundSchema,
};

export const tool$schemasPostv1SchemasNamespace: ToolDefinition<typeof args> = {
  name: "schemas-postv1-schemas-namespace",
  description: `Create a shared component

Create a new schema for the namespace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schemasPostv1SchemasNamespace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
