/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { themesDeletev1ThemesSlug } from "../../funcs/themesDeletev1ThemesSlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Deletev1ThemesSlugRequest$inboundSchema,
};

export const tool$themesDeletev1ThemesSlug: ToolDefinition<typeof args> = {
  name: "themes-deletev1-themes-slug",
  description: `Delete a theme

Delete a specific themes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await themesDeletev1ThemesSlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
