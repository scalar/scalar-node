/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { loginPortalsPostv1LoginPortals } from "../../funcs/loginPortalsPostv1LoginPortals.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Postv1LoginPortalsRequestBody$inboundSchema,
};

export const tool$loginPortalsPostv1LoginPortals: ToolDefinition<typeof args> =
  {
    name: "login-portals-postv1-login-portals",
    description: `Create a portal

Create a new login portal for the team`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await loginPortalsPostv1LoginPortals(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
