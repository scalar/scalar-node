/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { themesPatchv1ThemesSlug } from "../../funcs/themesPatchv1ThemesSlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Patchv1ThemesSlugRequest$inboundSchema,
};

export const tool$themesPatchv1ThemesSlug: ToolDefinition<typeof args> = {
  name: "themes-patchv1-themes-slug",
  description: `Update theme metadata

Update themes metadata`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await themesPatchv1ThemesSlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
