/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiDocsPostv1ApisNamespaceSlugAccessGroup } from "../../funcs/apiDocsPostv1ApisNamespaceSlugAccessGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Postv1ApisNamespaceSlugAccessGroupRequest$inboundSchema,
};

export const tool$apiDocsPostv1ApisNamespaceSlugAccessGroup: ToolDefinition<
  typeof args
> = {
  name: "API-docs-postv1-apis-namespace-slug-access-group",
  description: `Add API access group

Add an access group for the api document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiDocsPostv1ApisNamespaceSlugAccessGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
