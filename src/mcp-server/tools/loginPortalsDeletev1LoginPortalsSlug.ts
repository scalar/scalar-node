/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { loginPortalsDeletev1LoginPortalsSlug } from "../../funcs/loginPortalsDeletev1LoginPortalsSlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Deletev1LoginPortalsSlugRequest$inboundSchema,
};

export const tool$loginPortalsDeletev1LoginPortalsSlug: ToolDefinition<
  typeof args
> = {
  name: "login-portals-deletev1-login-portals-slug",
  description: `Delete a login portal

Delete a specific login portal`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await loginPortalsDeletev1LoginPortalsSlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
