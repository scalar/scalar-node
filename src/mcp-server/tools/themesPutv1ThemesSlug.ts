/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { themesPutv1ThemesSlug } from "../../funcs/themesPutv1ThemesSlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Putv1ThemesSlugRequest$inboundSchema,
};

export const tool$themesPutv1ThemesSlug: ToolDefinition<typeof args> = {
  name: "themes-putv1-themes-slug",
  description: `Update theme document

Update themes document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await themesPutv1ThemesSlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
