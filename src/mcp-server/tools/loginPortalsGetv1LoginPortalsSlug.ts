/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { loginPortalsGetv1LoginPortalsSlug } from "../../funcs/loginPortalsGetv1LoginPortalsSlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Getv1LoginPortalsSlugRequest$inboundSchema,
};

export const tool$loginPortalsGetv1LoginPortalsSlug: ToolDefinition<
  typeof args
> = {
  name: "login-portals-getv1-login-portals-slug",
  description: `Get a login portal

Get a login portal`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await loginPortalsGetv1LoginPortalsSlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
